apply plugin: 'com.android.library'

android {
    compileSdkVersion 21
    buildToolsVersion "22"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    compile 'com.android.support:recyclerview-v7:22.0.0' // Check WithLayerItemAnimator when updating.
}
